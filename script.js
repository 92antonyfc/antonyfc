const toggle=document.querySelector('.nav-toggle');const menu=document.querySelector('.nav-menu');if(toggle&&menu){toggle.addEventListener('click',()=>{const isOpen=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',String(isOpen));});menu.querySelectorAll('a').forEach(link=>{link.addEventListener('click',()=>{menu.classList.remove('open');toggle.setAttribute('aria-expanded','false');});});}document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',event=>{const targetId=anchor.getAttribute('href');if(!targetId||targetId==='#')return;const target=document.querySelector(targetId);if(!target)return;event.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',targetId);});});

document.querySelectorAll('[data-carousel]').forEach((carousel)=>{const img=carousel.querySelector('[data-carousel-image]');const prev=carousel.querySelector('.prev');const next=carousel.querySelector('.next');if(!img||!prev||!next)return;const base=img.dataset.base;const count=parseInt(img.dataset.count||'1',10);let index=1;const update=()=>{img.src=`./assets/${base}-${index}.jpg`;};prev.addEventListener('click',()=>{index=index<=1?count:index-1;update();});next.addEventListener('click',()=>{index=index>=count?1:index+1;update();});});
